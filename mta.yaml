_schema-version: 3.3.0
ID: cap_customers
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: cap_customers-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: cap_customers-auth
      - name: cap_customers-db
      - name: cap_customers-destination

  - name: cap_customers-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: cap_customers-db

  - name: cap_customers-app-deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: cap_customers-auth
      - name: srv-api
      - name: cap_customers-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: resources/
      requires:
        - name: cap_customersui_cap_customers
          artifacts:
            - ui_cap_customers.zip
          target-path: resources/
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: customersrv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet

  - name: cap_customersui_cap_customers
    type: html5
    path: app/ui_cap_customers
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: cap_customers-destinations
    type: com.sap.application.content
    requires:
      - name: cap_customers-auth
        parameters:
          service-key:
            name: cap_customers-auth-key
      - name: cap_customers-html5-repo-host
        parameters:
          service-key:
            name: cap_customers-html5-repo-host-key
      - name: srv-api
      - name: cap_customers-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: cap_customers-html5-repository
              ServiceInstanceName: cap_customers-html5-repo-host
              ServiceKeyName: cap_customers-html5-repo-host-key
              sap.cloud.service: cap_customers.service
            - Name: cap_customers-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: cap_customers-auth
              ServiceKeyName: cap_customers-auth-key
              sap.cloud.service: cap_customers.service

resources:
  - name: cap_customers-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: cap_customers-${org}-${space}
        tenant-mode: dedicated
        role-collections:
          - name: 'RiskManager (cap_customers ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.RiskManager'
  - name: cap_customers-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: cap_customers-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: cap_customers-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: cap_customers-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
